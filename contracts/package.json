{
  "name": "togibox-report-receipt-contracts",
  "version": "1.0.0",
  "description": "TogiboxReportReceipt ERC-721 contract — GIWA Sepolia (OP-Stack EVM L2)",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network giwaSepolia",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
